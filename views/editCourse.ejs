<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>Edit Course</h1>
  <img src="<%= course.imageURL %>" alt="">
  <% if(errors) { %>
    <p style="color:red"><%= errors %></p>
  <% } %> 
  <form action="/course/<%= id %>/edit" method="post">
    <div>
      <label for="">Name</label>
      <input type="text" name="name" value="<%= course.name %> ">
    </div>
    <div>
      <label for="" style="display:flex; flex-direction:column">Description</label>
      <textarea name="description" id="" cols="30" rows="10"><%= course.description %></textarea>
    </div>
    <div>
      <label for="">Video URL</label>
      <input type="text" name="videoURL" value="<%= course.videoURL %> ">
    </div>
    <div>
      <label for="">Category</label>
      <input type="text" name="category" value="<%= course.category %>">
    </div>
    <div>
      <label for="">Image URL</label>
      <input type="text" name="imageURL" value="<%= course.imageURL %> ">
    </div>
    <button type="submit">Save Changes</button>
  </form>
</body>
</html>